import { Routes } from '@angular/router';
import { HompageComponent } from '../home/hompage.component';

export const routes: Routes = [
    {
        path: '',
        component: HompageComponent
    }
];
